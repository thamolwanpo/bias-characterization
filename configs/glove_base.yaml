# Phase 1 Configuration: GloVe Base Model
# News encoder representation analysis on test set

# Model paths (UPDATE THESE)
poisoned_model_checkpoint: "/Users/ploymel/Documents/plm4newsrs/outputs/politifact/nrms_model/glove_300_frozen/checkpoints/poisoned-epoch=02-val_auc=0.6729.ckpt"
model_checkpoint: "/Users/ploymel/Documents/plm4newsrs/outputs/politifact/nrms_model/glove_300_frozen/checkpoints/clean-epoch=01-val_auc=0.4588.ckpt"
model_config: "/Users/ploymel/Documents/plm4newsrs/configs/experiments/nrms/nrms_glove.yaml"
model_type: "glove"  # Specify the model type

# Data paths
data_path: "/Users/ploymel/Documents/plm4newsrs/benchmarks/politifact/"  # Your test dataset

# Sampling (set to null to use all data)
sample_size: null  # Number of samples per class (fake/real)
seed: 42

# Processing
batch_size: 32
device: "cpu"  # GloVe works fine on CPU

# Output
output_dir: "outputs/phase1/glove_nrms"
save_embeddings: true  # Save raw embeddings for further analysis

# Visualization
tsne_max_samples: 5000  # Max samples for t-SNE (computational limit)