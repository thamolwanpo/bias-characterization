# Phase 1 Configuration: NRMS BERT Finetune Model
# News encoder representation analysis on test set

# Model paths (UPDATE THESE)
poisoned_model_checkpoint: "/content/drive/MyDrive/bias-characterized/outputs/politifact/nrms_model/bert_finetune/checkpoints/poisoned-epoch=06-val_auc=0.6574.ckpt"
model_checkpoint: "/content/drive/MyDrive/bias-characterized/outputs/politifact/nrms_model/bert_finetune/checkpoints/clean-epoch=00-val_auc=0.4738.ckpt"
model_config: "/content/drive/MyDrive/bias-characterized/bias-characterization/plm4newsrs/configs/experiments/nrms/nrms_bert_finetune.yaml"
model_type: "bert"  # Specify the model type

# Data paths
data_path: "/content/drive/MyDrive/bias-characterized/benchmarks/politifact/"  # Your test dataset

# Sampling (set to null to use all data)
sample_size: null  # Number of samples per class (fake/real)
seed: 42

# Processing
batch_size: 32
device: "cuda"  # GloVe works fine on CPU

# Output
output_dir: "/content/drive/MyDrive/bias-characterized/analysis/politifact/bert_nrms_finetune"
save_embeddings: true  # Save raw embeddings for further analysis

# Visualization
tsne_max_samples: 5000  # Max samples for t-SNE (computational limit)