# Phase 1 Configuration: NAML BERT Finetune Model
# News encoder representation analysis on test set

# Model paths (UPDATE THESE)
poisoned_model_checkpoint: "/workspace/plm4newsrs/outputs/politifact/naml_model/bert_finetune/checkpoints/poisoned-epoch=01-val_auc=0.7028.ckpt"
model_checkpoint: "/workspace/plm4newsrs/outputs/politifact/naml_model/bert_finetune/checkpoints/clean-epoch=01-val_auc=0.6497.ckpt"
model_config: "/workspace/plm4newsrs/configs/experiments/naml/naml_bert_finetune.yaml"
model_type: "bert"  # Specify the model type

# Data paths
data_path: "/workspace/plm4newsrs/data/politifact/benchmarks/"  # Your test dataset
news_items_path: "/workspace/plm4newsrs/data/politifact/news_items.csv"

# Sampling (set to null to use all data)
sample_size: null  # Number of samples per class (fake/real)
seed: 42

# Processing
batch_size: 32
device: "cuda"  # GloVe works fine on CPU

# Output
output_dir: "/workspace/plm4newsrs/bias-characterized/analysis/politifact/bert_naml_finetune"
save_embeddings: true  # Save raw embeddings for further analysis

# Visualization
tsne_max_samples: 5000  # Max samples for t-SNE (computational limit)